id: wifigpu_sync_long
label: sync long
category: '[WIFIGPU]'
templates:
  imports: import wifigpu
  make: wifigpu.sync_long(${sync_length})
parameters:
- id: sync_length
  label: Sync_length
  dtype: raw
  default: 320
inputs:
- label: in
  domain: stream
  dtype: complex
- label: delayed
  domain: stream
  dtype: complex
outputs:
- label: out
  domain: stream
  dtype: complex
file_format: 1
